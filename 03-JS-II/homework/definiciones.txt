for: es un bucle que nos permite realizar las acciones la cantidad de veces que queramos. Se comienza con for seguido de parentesis donde separados por ; comenzaremos definiendo la variable marcando desde que valor va a comenzar, luego escribimos la condicion que va a marcar el final del bucle y finalmente la forma en la que se hará el incremento de la variable. Luego dentro de llaves se escribiran las acciones que realizara el bucle.
Estos son los operadores logicos AND o Y (&&), OR o O (||) y NOT o NEGADO (!)  se utilizan para hacer operaciones con los datos booleanos, responden a la tabla de la verdad en donde: && devuelve true solo si las dos variables son true, de resto es false; || devuelve false solo si las dos variables son falsas, de resto siempre devuelve true y ! funciona como una especie de inverso de la variable convirtiendo un tru en false y viceversa.
